"use client"; import { useMemo, useState } from"react"; import Link from"next/link"; import { ArrowRight, ClipboardList, Copy, FolderOpen, LockKeyhole, Plus, Route, Search, } from"lucide-react"; import EOCPageShell from"@/components/layout/EOCPageShell"; import { getServiceRequestSearchText, serviceRequestDepartmentFilters, serviceRequestRecords, serviceRequestStatusFilters, type ServiceRequestPriority, type ServiceRequestRecord, type ServiceRequestStatus, } from"@/data/serviceRequests"; export default function ServiceRequestsPage() { const [selectedRequest, setSelectedRequest] = useState<ServiceRequestRecord | null>(null); const [searchTerm, setSearchTerm] = useState(""); const [statusFilter, setStatusFilter] = useState("All Statuses"); const [departmentFilter, setDepartmentFilter] = useState("All Departments"); const [copiedId, setCopiedId] = useState<string | null>(null); const filteredRequests = useMemo(() => { const normalizedSearch = searchTerm.trim().toLowerCase(); return serviceRequestRecords.filter((request) => { const matchesSearch = !normalizedSearch || getServiceRequestSearchText(request).includes(normalizedSearch); const matchesStatus = statusFilter ==="All Statuses" || request.status === statusFilter; const matchesDepartment = departmentFilter ==="All Departments" || request.department === departmentFilter; return matchesSearch && matchesStatus && matchesDepartment; }); }, [departmentFilter, searchTerm, statusFilter]); const statCards = [ { label:"Service Requests", value: serviceRequestRecords.length.toString(), icon: ClipboardList, }, { label:"Open Intake", value: serviceRequestRecords .filter((request) => request.status !=="Closed") .length.toString(), icon: FolderOpen, }, { label:"Pending Routing", value: serviceRequestRecords .filter((request) => request.status ==="Pending Routing") .length.toString(), icon: Route, }, { label:"Restricted Review", value: serviceRequestRecords .filter((request) => request.status ==="Restricted Review") .length.toString(), icon: LockKeyhole, }, ]; function clearFilters() { setSearchTerm(""); setStatusFilter("All Statuses"); setDepartmentFilter("All Departments"); } function copyRequestId(requestId: string) { if (typeof navigator !=="undefined" && navigator.clipboard) { void navigator.clipboard.writeText(requestId); } setCopiedId(requestId); window.setTimeout(() => { setCopiedId(null); }, 1800); } return ( <EOCPageShell> <section className="rounded-xl bg-[#050816] p-7 text-white shadow-md ring-1 ring-slate-800 lg:p-9"> <div className="grid gap-6 lg:grid-cols-[minmax(0,1fr)_260px] lg:items-center"> <div> <p className="text-xs font-black uppercase tracking-[0.45em] text-[#ffc400]"> Hassan Industries </p> <h1 className="mt-5 text-3xl font-black uppercase tracking-tight lg:text-4xl"> Service Requests Desk </h1> <p className="mt-4 max-w-5xl text-sm font-semibold leading-7 text-white"> Universal frontend intake and routing workspace for administrative requests, governance review, document-control actions, certified-copy support, treasury support, and restricted HCA review preparation. </p> </div> <div className="rounded-lg border border-[#ffb000] bg-slate-900 p-6 text-center"> <p className="text-xs font-black uppercase tracking-[0.45em] text-white"> Module Status </p> <p className="mt-5 text-2xl font-black text-[#ffc400]"> Frontend Desk </p> <p className="mt-2 text-xs font-black text-white"> Intake Routing Layer </p> </div> </div> </section> <div className="flex flex-wrap gap-3"> <Link href="/" className="inline-flex items-center gap-2 rounded-lg border border-slate-300 bg-white px-5 py-3 text-sm font-black shadow-sm transition hover:border-[#ff8a00] hover:bg-amber-50" > <FolderOpen size={16} className="text-[#ff8a00]" /> Back to Dashboard </Link> <Link href="/service-requests/queues" className="inline-flex items-center gap-2 rounded-lg border border-slate-300 bg-white px-5 py-3 text-sm font-black shadow-sm transition hover:border-[#ff8a00] hover:bg-amber-50" > <Route size={16} className="text-[#ff8a00]" /> Routing Queues </Link> </div> <p className="text-right text-xs font-black uppercase tracking-[0.45em] text-slate-400"> Universal Intake & Routing </p> <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"> {statCards.map((stat) => { const Icon = stat.icon; return ( <div key={stat.label} className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm" > <div className="flex items-center justify-between gap-4"> <div> <p className="text-sm font-bold text-slate-600">{stat.label}</p> <p className="mt-2 text-3xl font-black">{stat.value}</p> </div> <Icon size={24} className="text-[#ff8a00]" /> </div> </div> ); })} </section> <section className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"> <div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_220px_260px_140px]"> <label className="block"> <span className="text-xs font-black uppercase tracking-[0.35em] text-slate-500"> Search Requests </span> <div className="mt-2 flex h-12 items-center gap-3 rounded-lg border border-slate-300 bg-white px-4"> <Search size={17} className="text-slate-400" /> <input value={searchTerm} onChange={(event) => setSearchTerm(event.target.value)} placeholder="Search by request ID, title, department, owner, category, classification, or status..." className="h-full w-full bg-transparent text-sm outline-none placeholder:text-slate-400" /> </div> </label> <label className="block"> <span className="text-xs font-black uppercase tracking-[0.35em] text-slate-500"> Status </span> <select value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)} className="mt-2 h-12 w-full rounded-lg border border-slate-300 bg-white px-4 text-sm font-bold outline-none transition focus:border-amber-500" > {serviceRequestStatusFilters.map((status) => ( <option key={status}>{status}</option> ))} </select> </label> <label className="block"> <span className="text-xs font-black uppercase tracking-[0.35em] text-slate-500"> Department </span> <select value={departmentFilter} onChange={(event) => setDepartmentFilter(event.target.value)} className="mt-2 h-12 w-full rounded-lg border border-slate-300 bg-white px-4 text-sm font-bold outline-none transition focus:border-amber-500" > {serviceRequestDepartmentFilters.map((department) => ( <option key={department}>{department}</option> ))} </select> </label> <button type="button" onClick={clearFilters} className="mt-6 h-12 rounded-lg border border-slate-300 bg-white px-4 text-sm font-black shadow-sm transition hover:border-[#ff8a00] hover:bg-amber-50 xl:mt-auto" > Clear Filters </button> </div> </section> <section className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_400px]"> <div className="min-w-0 rounded-xl border border-slate-200 bg-white shadow-sm"> <div className="flex flex-wrap items-start justify-between gap-4 border-b border-slate-200 p-6"> <div> <p className="text-xs font-black uppercase tracking-[0.35em] text-slate-400"> Enterprise Operations System </p> <h2 className="mt-2 text-2xl font-black"> Service Requests Registry </h2> <p className="mt-3 max-w-3xl text-sm leading-7 text-slate-600"> Select a service request to preview intake authority, routing stage, department ownership, classification, and future workflow controls. </p> </div> <div className="flex items-center gap-3"> <span className="rounded-full bg-amber-100 px-5 py-3 text-xs font-black text-amber-700"> {filteredRequests.length} shown </span> <Link href="/service-requests/new" className="inline-flex items-center gap-2 rounded-lg bg-[#050816] px-5 py-3 text-sm font-black text-white transition hover:bg-[#111827]" > <Plus size={16} className="text-[#ffc400]" /> Create Service Request </Link> </div> </div> <div className="overflow-x-auto"> <table className="min-w-[980px] w-full text-left"> <thead className="bg-slate-50 text-xs font-black uppercase tracking-[0.35em] text-slate-500"> <tr> <th className="px-6 py-4">Request ID</th> <th className="px-6 py-4">Title</th> <th className="px-6 py-4">Department</th> <th className="px-6 py-4">Status</th> <th className="px-6 py-4">Priority</th> <th className="px-6 py-4">Owner</th> <th className="px-6 py-4">Queue</th> <th className="px-6 py-4">Actions</th> </tr> </thead> <tbody> {filteredRequests.map((request) => { const isSelected = selectedRequest?.id === request.id; return ( <tr key={request.id} onClick={() => setSelectedRequest(request)} className={["cursor-pointer border-b border-slate-200 transition", isSelected ?"bg-amber-50" :"bg-white hover:bg-slate-50", ].join("")} > <td className="px-6 py-6 align-top text-sm font-black"> {request.id} </td> <td className="px-6 py-6 align-top"> <p className="text-sm font-black">{request.title}</p> <p className="mt-2 max-w-[260px] text-xs leading-6 text-slate-600"> {request.summary} </p> </td> <td className="px-6 py-6 align-top text-sm font-bold text-slate-700"> {request.department} </td> <td className="px-6 py-6 align-top"> <span className={["rounded-md px-3 py-2 text-xs font-black", getStatusPillClass(request.status), ].join("")} > {request.status} </span> </td> <td className="px-6 py-6 align-top"> <span className={["rounded-md px-3 py-2 text-xs font-black", getPriorityPillClass(request.priority), ].join("")} > {request.priority} </span> </td> <td className="px-6 py-6 align-top text-sm font-black"> {request.owner} </td> <td className="px-6 py-6 align-top"> <Link href={`/service-requests/queues/${request.routingQueueId}`} onClick={(event) => event.stopPropagation()} className="text-xs font-black text-blue-700 hover:text-amber-600" > {request.assignedQueue} </Link> </td> <td className="px-6 py-6 align-top"> <Link href={`/service-requests/${request.id}`} onClick={(event) => event.stopPropagation()} className="inline-flex items-center gap-2 rounded-lg border border-slate-300 bg-white px-3 py-2 text-xs font-black text-slate-950 transition hover:border-amber-500 hover:bg-amber-50" > Open <ArrowRight size={14} className="text-[#ff8a00]" /> </Link> </td> </tr> ); })} </tbody> </table> </div> </div> {selectedRequest ? ( <SelectedRequestPanel request={selectedRequest} copiedId={copiedId} onCopyRequestId={() => copyRequestId(selectedRequest.id)} /> ) : ( <EmptySelectionPanel /> )} </section> <section className="grid gap-6 xl:grid-cols-2"> <InfoCard title="Routing Standard"> <ul className="space-y-3 text-sm leading-7 text-slate-600"> <li>• Service requests serve as universal intake.</li> <li>• Every request should have an assigned queue.</li> <li>• HCA review remains separated from execution.</li> <li>• Restricted matters require controlled visibility.</li> </ul> </InfoCard> <InfoCard title="Backend Readiness"> <p className="text-sm leading-7 text-slate-600"> Future work should connect this desk to authenticated request submission, role-based assignment, department queues, service-level tracking, comments, attachments, audit logs, approval routing, and permanent records. </p> </InfoCard> </section> </EOCPageShell> ); } function EmptySelectionPanel() { return ( <aside className="min-w-0 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"> <div className="flex items-start justify-between gap-4"> <div> <p className="text-xs font-black uppercase tracking-[0.35em] text-slate-400"> Request Preview </p> <h2 className="mt-2 text-2xl font-black">No Request Selected</h2> <p className="mt-4 text-sm leading-7 text-slate-600"> Select a service request record to review intake category, routing stage, ownership, classification, and future workflow controls. </p> </div> <div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-lg bg-[#050816] text-[#ffc400]"> <ClipboardList size={22} /> </div> </div> <div className="mt-6 rounded-lg bg-[#050816] p-5 text-white"> <p className="text-xs font-black uppercase tracking-[0.35em]"> Service Request Workspace </p> <p className="mt-3 text-sm font-bold leading-6"> Request metadata opens only after intentional request selection. </p> </div> </aside> ); } function SelectedRequestPanel({ request, copiedId, onCopyRequestId, }: { request: ServiceRequestRecord; copiedId: string | null; onCopyRequestId: () => void; }) { return ( <aside className="min-w-0 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"> <div className="flex items-start justify-between gap-4"> <div> <p className="text-xs font-black uppercase tracking-[0.35em] text-slate-400"> Selected Request </p> <h2 className="mt-2 text-2xl font-black">{request.id}</h2> <p className="mt-2 text-sm font-black">{request.title}</p> </div> <span className={["rounded-md px-3 py-2 text-xs font-black", getStatusPillClass(request.status), ].join("")} > {request.status} </span> </div> <div className="mt-6 rounded-lg bg-[#050816] p-5 text-white"> <p className="text-xs font-black uppercase tracking-[0.35em]"> Controlled Request </p> <p className="mt-2 text-sm font-bold">{request.classification}</p> </div> <div className="mt-5"> {[ ["Requester", request.requester], ["Department", request.department], ["Owner", request.owner], ["Category", request.category], ["Priority", request.priority], ["Stage", request.stage], ["Assigned Queue", request.assignedQueue], ["Due Date", request.dueDate], ["Related Record", request.relatedRecord], ].map(([label, value]) => ( <DetailRow key={label} label={label} value={value} /> ))} </div> <div className="mt-5 rounded-lg border border-slate-200 bg-slate-50 p-5"> <p className="text-xs font-black uppercase tracking-[0.35em] text-slate-500"> Routing Note </p> <p className="mt-3 text-sm leading-7 text-slate-700"> {request.routingNote} </p> </div> <div className="mt-5 space-y-3"> <button type="button" onClick={onCopyRequestId} className="flex w-full items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white px-4 py-3 text-sm font-black transition hover:border-[#ff8a00] hover:bg-amber-50" > <Copy size={16} className="text-[#ff8a00]" /> {copiedId === request.id ?"Request ID Copied" :"Copy Request ID"} </button> <Link href={`/service-requests/${request.id}`} className="flex w-full items-center justify-center gap-2 rounded-lg bg-[#050816] px-4 py-3 text-sm font-black text-white transition hover:bg-[#111827]" > Open Detail Workspace <ArrowRight size={16} className="text-[#ffc400]" /> </Link> <Link href={`/service-requests/queues/${request.routingQueueId}`} className="flex w-full items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white px-4 py-3 text-sm font-black transition hover:border-[#ff8a00] hover:bg-amber-50" > Open Assigned Queue <Route size={16} className="text-[#ff8a00]" /> </Link> </div> </aside> ); } function InfoCard({ title, children, }: { title: string; children: React.ReactNode; }) { return ( <section className="rounded-xl border border-dashed border-slate-300 bg-white p-6 shadow-sm"> <h3 className="text-xl font-black uppercase tracking-[0.25em]">{title}</h3> <div className="mt-5">{children}</div> </section> ); } function DetailRow({ label, value }: { label: string; value: string }) { return ( <div className="flex items-start justify-between gap-4 border-b border-slate-200 py-3 text-sm"> <span className="font-bold text-slate-500">{label}</span> <span className="text-right font-black text-[#050816]">{value}</span> </div> ); } function getStatusPillClass(status: ServiceRequestStatus) { switch (status) { case"Open": return"bg-emerald-100 text-emerald-700"; case"In Review": return"bg-blue-100 text-blue-700"; case"Pending Routing": return"bg-amber-100 text-amber-700"; case"Restricted Review": return"bg-rose-100 text-rose-700"; case"Closed": return"bg-slate-200 text-slate-600"; default: return"bg-slate-200 text-slate-600"; } } function getPriorityPillClass(priority: ServiceRequestPriority) { switch (priority) { case"Normal": return"bg-slate-100 text-slate-700"; case"High": return"bg-amber-100 text-amber-700"; case"Restricted": return"bg-rose-100 text-rose-700"; default: return"bg-slate-100 text-slate-700"; } }