import Link from"next/link"; import { ArrowRight, ClipboardList, FolderOpen, LockKeyhole, Plus, Route, ShieldCheck, } from"lucide-react"; import ServiceRequestWorkspaceFrame from"@/components/service-requests/ServiceRequestWorkspaceFrame"; import { getRequestsForQueue, serviceRequestQueues, serviceRequestRecords, type ServiceRequestQueueStatus, } from"@/data/serviceRequests"; export default function ServiceRequestQueuesPage() { const queueCount = serviceRequestQueues.length; const restrictedQueueCount = serviceRequestQueues.filter( (queue) => queue.status ==="Restricted Queue", ).length; const departmentQueueCount = serviceRequestQueues.filter( (queue) => queue.status ==="Department Queue", ).length; const activeRequestCount = serviceRequestRecords.filter( (request) => request.status !=="Closed", ).length; const statCards = [ { label:"Routing Queues", value: queueCount.toString(), icon: Route }, { label:"Active Requests", value: activeRequestCount.toString(), icon: ClipboardList, }, { label:"Department Queues", value: departmentQueueCount.toString(), icon: FolderOpen, }, { label:"Restricted Queues", value: restrictedQueueCount.toString(), icon: LockKeyhole, }, ]; return ( <ServiceRequestWorkspaceFrame> <section className="rounded-xl bg-[#050816] p-8 text-white shadow-sm"> <div className="flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between"> <div> <p className="text-xs font-black uppercase tracking-[0.45em] text-[#ffc400]"> Hassan Industries </p> <h1 className="mt-4 text-3xl font-black uppercase"> Service Request Routing Queues </h1> <p className="mt-4 max-w-5xl text-sm leading-7 text-white"> Controlled routing layer for assigning service requests to department desks, HCA review lanes, records review, treasury review, and restricted governance handling. </p> </div> <div className="rounded-lg border border-[#ff8a00] bg-white/5 px-10 py-7 text-center"> <p className="text-xs font-black uppercase tracking-[0.35em]"> Queue Status </p> <p className="mt-4 text-2xl font-black text-[#ffc400]"> Frontend Queues </p> <p className="mt-1 text-xs font-black">Assignment Layer</p> </div> </div> </section> <div className="mt-5 flex flex-wrap gap-3"> <Link href="/service-requests" className="service-button"> Back to Service Requests Desk </Link> <Link href="/service-requests/new" className="service-button-primary"> <Plus size={16} /> Create Service Request </Link> </div> <p className="mt-8 text-right text-xs font-black uppercase tracking-[0.45em] text-slate-400"> Controlled Queue Navigation </p> <section className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"> {statCards.map((stat) => { const Icon = stat.icon; return ( <div key={stat.label} className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm" > <div className="flex items-start justify-between"> <div> <p className="text-sm font-bold text-slate-500">{stat.label}</p> <p className="mt-2 text-3xl font-black">{stat.value}</p> </div> <Icon className="text-[#ff8a00]" size={24} /> </div> </div> ); })} </section> <section className="mt-5 grid gap-5 xl:grid-cols-[1fr_390px]"> <div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"> <div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"> <div> <p className="text-xs font-black uppercase tracking-[0.35em] text-slate-400"> Enterprise Operations System </p> <h2 className="mt-2 text-2xl font-black"> Controlled Routing Queues </h2> <p className="mt-3 max-w-4xl text-sm leading-7 text-slate-600"> Each queue has a defined purpose, owner, access scope, routing standard, and escalation path so requests do not sit in an ungoverned general inbox. </p> </div> <span className="rounded-full bg-amber-100 px-5 py-3 text-sm font-black text-amber-700"> {serviceRequestQueues.length} queues </span> </div> <div className="mt-6 grid gap-4 lg:grid-cols-2"> {serviceRequestQueues.map((queue) => { const queueRequests = getRequestsForQueue(queue.id); return ( <Link key={queue.id} href={`/service-requests/queues/${queue.id}`} className="group rounded-xl border border-slate-200 bg-slate-50 p-6 transition hover:border-[#ff8a00] hover:bg-amber-50" > <div className="flex items-start justify-between gap-4"> <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-[#050816] text-[#ffc400]"> <Route size={24} /> </div> <span className={`rounded-full px-4 py-2 text-xs font-black ${getQueueStatusClass( queue.status, )}`} > {queue.status} </span> </div> <h3 className="mt-6 text-xl font-black">{queue.title}</h3> <p className="mt-4 min-h-[72px] text-sm leading-7 text-slate-600"> {queue.summary} </p> <div className="mt-5 flex items-center justify-between border-t border-slate-200 pt-4"> <span className="text-xs font-black uppercase tracking-[0.35em] text-slate-400"> {queueRequests.length} assigned </span> <span className="inline-flex items-center gap-2 text-sm font-black"> Open Queue <ArrowRight size={16} className="text-[#ff8a00] transition group-hover:translate-x-1" /> </span> </div> </Link> ); })} </div> </div> <aside className="space-y-5"> <InfoPanel icon={<ShieldCheck size={24} />} title="Queue Standard" text="Each queue has a defined owner, governed routing purpose, escalation path, and separation standard for restricted matters." /> <InfoPanel icon={<ClipboardList size={24} />} title="Backend Readiness" text="Future backend work should connect queues to role-based permissions, assignment rules, SLA timers, escalations, notifications, comments, attachments, approvals, and permanent service request audit history." /> <InfoPanel icon={<Route size={24} />} title="Training Note" text="Queue pages should be treated as operating desks, not passive lists. Every queue should help staff determine ownership, review path, escalation, and next action." /> </aside> </section> </ServiceRequestWorkspaceFrame> ); } function InfoPanel({ icon, title, text, }: { icon: React.ReactNode; title: string; text: string; }) { return ( <section className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"> <div className="flex items-center gap-4"> <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-[#050816] text-[#ffc400]"> {icon} </div> <h3 className="text-xl font-black uppercase tracking-[0.25em]"> {title} </h3> </div> <p className="mt-5 text-sm leading-7 text-slate-600">{text}</p> </section> ); } function getQueueStatusClass(status: ServiceRequestQueueStatus) { switch (status) { case"Active Queue": return"bg-emerald-100 text-emerald-700"; case"Department Queue": return"bg-blue-100 text-blue-700"; case"Restricted Queue": return"bg-rose-100 text-rose-700"; default: return"bg-slate-100 text-slate-700"; } }