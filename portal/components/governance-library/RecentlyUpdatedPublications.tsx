"use client"; import Link from"next/link"; import type { Dispatch, SetStateAction } from"react"; import type { PublicationRecord } from"@/data/governanceLibrary"; import { getPublicationDocumentNumber } from"@/lib/eglPublicationRecords"; interface RecentlyUpdatedPublicationsProps { publications: PublicationRecord[]; selectedPublication?: PublicationRecord | null; setSelectedPublication?: Dispatch<SetStateAction<PublicationRecord | null>>; onSelectPublication?: (publication: PublicationRecord) => void; } type DisplayPublicationRecord = PublicationRecord & { id?: string; documentId?: string; documentNo?: string; documentNumber?: string; title?: string; name?: string; description?: string; summary?: string; series?: string; publicationSeries?: string; category?: string; owner?: string; version?: string | number; status?: string; reviewDate?: string; lastUpdated?: string; updatedAt?: string; }; export default function RecentlyUpdatedPublications({ publications, selectedPublication = null, setSelectedPublication, onSelectPublication, }: RecentlyUpdatedPublicationsProps) { const records = publications.filter(isPublicationRecord); const selectedKey = selectedPublication ? getPublicationKey(selectedPublication as DisplayPublicationRecord) : null; function handleSelect(record: PublicationRecord) { if (setSelectedPublication) { setSelectedPublication(record); } if (onSelectPublication) { onSelectPublication(record); } } return ( <section className="rounded-lg border border-slate-200 bg-white shadow-sm"> <div className="flex items-start justify-between gap-4 border-b border-slate-200 p-5"> <div> <p className="text-[11px] font-black uppercase tracking-[0.35em] text-slate-400"> Enterprise Governance Library </p> <h2 className="mt-2 text-xl font-black text-slate-950"> Recently Updated Publications </h2> </div> <Link href="/governance-library/publications" className="text-xs font-black text-blue-700 transition hover:text-amber-600" > View All Publications → </Link> </div> <div className="overflow-x-auto p-5"> <table className="w-full min-w-[760px] text-left text-sm"> <thead> <tr className="border-b border-slate-200 bg-slate-50 text-[11px] font-black uppercase tracking-[0.25em] text-slate-500"> <th className="px-3 py-3">Document No.</th> <th className="px-3 py-3">Title</th> <th className="px-3 py-3">Series</th> <th className="px-3 py-3">Status</th> <th className="px-3 py-3">Version</th> <th className="px-3 py-3">Owner</th> <th className="px-3 py-3">Review Date</th> </tr> </thead> <tbody> {records.map((publication) => { const record = publication as DisplayPublicationRecord; const recordKey = getPublicationKey(record); const isSelected = selectedKey === recordKey; return ( <tr key={recordKey} tabIndex={0} role="button" onClick={() => handleSelect(publication)} onKeyDown={(event) => { if (event.key ==="Enter" || event.key ==="") { event.preventDefault(); handleSelect(publication); } }} className={["cursor-pointer border-b border-slate-200 transition last:border-b-0", isSelected ?"bg-amber-50" :"bg-white hover:bg-slate-50", ].join("")} > <td className="px-3 py-4 align-top font-black text-slate-950"> {getPublicationDocumentNumber(record)} </td> <td className="px-3 py-4 align-top"> <p className="font-black text-slate-950"> {getTitle(record)} </p> <p className="mt-2 max-w-xs text-xs leading-5 text-slate-500"> {getDescription(record)} </p> </td> <td className="px-3 py-4 align-top font-bold text-slate-700"> {getSeries(record)} </td> <td className="px-3 py-4 align-top"> <span className="inline-flex rounded-md bg-emerald-100 px-2 py-1 text-xs font-black text-emerald-700"> {getStatus(record)} </span> </td> <td className="px-3 py-4 align-top font-black text-slate-950"> {String(record.version ??"1.0")} </td> <td className="px-3 py-4 align-top font-black text-slate-950"> {record.owner ??"HCA"} </td> <td className="px-3 py-4 align-top font-bold text-slate-700"> {record.reviewDate ?? record.lastUpdated ??"Pending"} </td> </tr> ); })} {records.length === 0 && ( <tr> <td colSpan={7} className="px-3 py-8 text-center"> <p className="font-black text-slate-950"> No recently updated publications available. </p> <p className="mt-2 text-sm text-slate-500"> Future backend integration will populate this table from the publication registry and document activity log. </p> </td> </tr> )} </tbody> </table> </div> <div className="border-t border-slate-200 px-5 py-3"> <p className="text-xs text-slate-500"> Select a publication row to update the Document Profile panel. </p> </div> </section> ); } function isPublicationRecord( record: PublicationRecord | null | undefined, ): record is PublicationRecord { return Boolean(record); } function getPublicationKey(record: DisplayPublicationRecord) { return ( record.id ?? record.documentId ?? record.documentNo ?? record.documentNumber ?? getPublicationDocumentNumber(record) ?? getTitle(record) ); } function getTitle(record: DisplayPublicationRecord) { return record.title ?? record.name ??"Untitled Publication"; } function getDescription(record: DisplayPublicationRecord) { return ( record.description ?? record.summary ??"Controlled enterprise publication record." ); } function getSeries(record: DisplayPublicationRecord) { return ( record.series ?? record.publicationSeries ?? record.category ??"Administration" ); } function getStatus(record: DisplayPublicationRecord) { return record.status ??"AP"; }