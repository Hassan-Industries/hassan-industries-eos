"use client"; import type { ReactNode } from"react"; import { useState } from"react"; import { Check, Copy } from"lucide-react"; interface CopyToClipboardButtonProps { value: string; label?: string; copiedLabel?: string; className?: string; children?: ReactNode; } const defaultButtonClassName ="flex h-12 w-full items-center justify-center gap-2 rounded-lg border border-[#c8d3df] bg-white px-4 text-sm font-black text-[#050816] transition hover:border-[#ff8a00] hover:text-[#ff8a00]"; export default function CopyToClipboardButton({ value, label ="Copy", copiedLabel ="Copied", className, children, }: CopyToClipboardButtonProps) { const [copied, setCopied] = useState(false); async function handleCopy() { try { if (navigator.clipboard?.writeText) { await navigator.clipboard.writeText(value); } else { fallbackCopy(value); } showCopiedState(); } catch { fallbackCopy(value); showCopiedState(); } } function showCopiedState() { setCopied(true); window.setTimeout(() => { setCopied(false); }, 1800); } return ( <button type="button" onClick={handleCopy} className={className ?? defaultButtonClassName} aria-label={copied ? copiedLabel : label} title={copied ? copiedLabel : label} > {children ?? ( <> {copied ? ( <Check size={16} className="text-emerald-600" /> ) : ( <Copy size={16} className="text-[#ff8a00]" /> )} <span>{copied ? copiedLabel : label}</span> </> )} </button> ); } function fallbackCopy(value: string) { const textarea = document.createElement("textarea"); textarea.value = value; textarea.setAttribute("readonly",""); textarea.style.position ="fixed"; textarea.style.left ="-9999px"; document.body.appendChild(textarea); textarea.select(); document.execCommand("copy"); document.body.removeChild(textarea); }